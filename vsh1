#version 300 es
layout (location = 0) in vec3 aPos;
layout (location = 1) in vec2 aTex;
uniform float pan;
out vec2 TexCoords;
void main() {
	gl_Position = vec4(aPos, 1.0);
	TexCoords = vec2(aTex.x + pan, aTex.y);
}
